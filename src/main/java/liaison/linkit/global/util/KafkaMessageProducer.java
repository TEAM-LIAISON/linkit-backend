package liaison.linkit.global.util;

import org.springframework.stereotype.Service;

@Service
public class KafkaMessageProducer {
//    private final KafkaTemplate<String, MessageKafka> kafkaTemplate;
//
//    public KafkaMessageProducer(KafkaTemplate<String, MessageKafka> kafkaTemplate) {
//        this.kafkaTemplate = kafkaTemplate;
//    }
//
//    public void sendMessage(MessageKafka message, String topic) {
//        kafkaTemplate.send(topic, message);
//    }
}
