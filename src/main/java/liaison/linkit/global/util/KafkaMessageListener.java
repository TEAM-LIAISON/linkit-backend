package liaison.linkit.global.util;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;

@Service
@Slf4j
public class KafkaMessageListener {

//    @KafkaListener(topics = "chat-room-{chatRoomId}", groupId = "chat_group")
//    public void consumeMessage(MessageKafka message) {
//        log.info("Received message: {}", message);
//        // Process the received message (e.g., store in DB, broadcast to clients)
//    }
}
