spring:
  profiles:
    active: local
    group:
      local: local, common, secret

server:
  env: local

---

spring:
  config:
    activate:
      on-profile: local

server:
  port: 8080

---

spring:
  config:
    activate:
      on-profile: common

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: false
        show_sql: false
        use_sql_comments: false
        default_batch_fetch_size: 100
    hibernate:
      ddl-auto: create

  mvc:
    path match:
      matching-strategy: ant_path_matcher

  batch:
    job:
      enabled: true
    jdbc:
      initialize-schema: always

    #  flyway:
    #    enabled: true
    #    baseline-on-migrate: true
    #    baseline-version: 0
  sql:
    init:
      mode: never

  servlet:
    multipart:
      enabled: true # 멀티파트 업로드 지원여부
      file-size-threshold: 0B # 파일을 디스크에 저장하지 않고 메모리에 저장하는 최소 크기 (default: 0B)
      max-file-size: 2MB # 한개 파일의 최대 사이즈 (default: 1MB)
      max-request-size: 2MB # 한개 요청의 최대 사이즈 (default: 10MB)

cors-allowed-origins:
  http://localhost:8080,
  http://localhost:3000,
  https://linkit.im,
  https://www.linkit.im,
  https://liaison-dev.site,
  https://www.liaison-dev.site,
  https://linkit-dev.vercel.app

---

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/linkit_dev_db
    username: root
    password: kk040802
    driver-class-name: com.mysql.cj.jdbc.Driver

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 58cd88d857ed286cce69f4516fb1a5e3
            client-secret: RkXkbK0vl0sxdcPq002YOOgSJRThc1w5
            redirect-uri: https://liaison-dev.site/login/oauth2/callback/kakao
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            scope:
              - profile_nickname
              - profile_image
            client-name: Kakao
          naver:
            client-id: bcCOWeo_guWTrh6E4dZc
            client-secret: APCsMvDwpF
            redirect-uri: https://liaison-dev.site/login/oauth2/callback/naver
            authorization-grant-type: authorization_code
            scope: name, email
            client-name: Naver
          google:
            client-id: 866779465778-qhte7gckuo2320cfa36kp3vuv579t0d6.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: http://localhost:3000/login/oauth2/callback/google
            scope:
              - email
              - profile
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-info-authentication-method: header
            user-name-attribute: id
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          google:
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v2/userinfo
  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: linkit@linkit.im
    password: fatu mxkw wcuy pkdf
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 5000
      mail.smtp.writetimeout: 5000

cloud:
  aws:
    credentials:
      access-key: AKIAXYKJXDXTWVCCGJGE
      secret-key: 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
    region:
      static: ap-northeast-1
    s3:
      bucket: linkit-dev-env-bucket
      image-folder: images/
      file-folder: files/
      cloud-front-image-domain: image.linkit.im
      cloud-front-file-domain: file.linkit.im
    stack:
      auto: false

jwt:
  secret: dGVhbWJ1aWxkaW5nLW1hdGhjaW5nLXNlcnZpY2UtYXMtcGxhdGZvcm0tc3ByaW5nLWJvb3Qtc2VydmVyLXNlcmNyZXQtdG9rZW4\\=
  access-expiration-time: 3600000
  refresh-expiration-time: 7200000

university:
  csv-path: csv/university.csv

region:
  csv-path: csv/region.csv

scale:
  csv-path: csv/scale.csv

skill:
  csv-path: csv/skill.csv

jobRole:
  csv-path: csv/jobRole.csv

position:
  csv-path: csv/position.csv

profileState:
  csv-path: csv/profileState.csv

teamState:
  csv-path: csv/teamState.csv

google:
  id: linkit@linkit.im
  password: fatu mxkw wcuy pkdf
